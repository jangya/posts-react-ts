import React from "react";

function Header() {
    return (
        <nav className="navbar navbar-dark sticky-top bg-dark bg-gradient">
            <div className="container">
                <span className="navbar-brand mb-0 h1">My Posts</span>
            </div>
        </nav>
    );
}

export default Header;
